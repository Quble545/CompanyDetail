import { Container } from "@material-ui/core";

import Form from "./Components/Form";

const App = () => {
  return (
    <Container maxWidth="lg">
      <Form />
    </Container>
  );
};

export default App;
